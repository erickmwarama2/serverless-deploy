service: hello-serverless

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-2


functions:
  hello:
    handler: handler.hello
  add:
    handler: add.add
    events:
      - http:
          path: math/add
          method: post
          cors: true

